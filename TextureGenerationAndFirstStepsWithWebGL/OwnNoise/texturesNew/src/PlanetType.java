public enum PlanetType {
    //used to define the type of texture that should be created, effects which color gradient gets chosen
    //to map the noise value to a color
    ICE_PLANET, LAVA_PLANET, STAR, BLACK_AND_WHITE;
}
